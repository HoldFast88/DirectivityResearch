using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

namespace Mic
{
    enum MicType
    {
        MicTypeAllDirectional,
        MicTypeOrgan
    };

    class DirectivityController
    {
        public static float CalculateDirectivityOrganType(uint frequency, uint count, uint tubesNumber, double deltha)
        {
            return 0.0F;
        }

        ///////////////////////////////////////////////////////////////////////////////////////////////
    }
}
